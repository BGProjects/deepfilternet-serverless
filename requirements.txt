# DeepFilterNet3 Serverless Requirements  
# Optimized for RunPod RTX 5090 deployment with CUDA 12.8

# Core Dependencies
runpod>=1.6.0
loguru>=0.7.0
numpy>=1.24.0,<2.0.0

# Audio Processing
soundfile>=0.12.1
scipy>=1.10.0

# ONNX Runtime (GPU version installed separately in Dockerfile)
# onnxruntime-gpu==1.18.1  # Installed in Dockerfile with CUDA 12.4 support

# PyTorch (installed separately in Dockerfile as nightly build for RTX 5090 sm_120 support)
# torch>=2.8.0 # Installed in Dockerfile from nightly builds
# torchaudio>=2.8.0 # Installed in Dockerfile from nightly builds

# Additional Utilities  
requests>=2.28.0
psutil>=5.9.0
packaging>=21.0

# Development/Testing (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0

# Performance Monitoring
py-cpuinfo>=9.0.0