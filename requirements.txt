# DeepFilterNet3 Serverless Requirements
# Optimized for RunPod RTX 4090 deployment

# Core Dependencies
runpod>=1.6.0
loguru>=0.7.0
numpy>=1.24.0,<2.0.0

# Audio Processing
soundfile>=0.12.1
scipy>=1.10.0

# ONNX Runtime (GPU version installed separately in Dockerfile)
# onnxruntime-gpu==1.16.3  # Installed in Dockerfile with CUDA 12.1 support

# PyTorch (for GPU utilities and tensor operations)
torch>=2.0.0,<2.2.0
torchaudio>=2.0.0,<2.2.0

# Additional Utilities  
requests>=2.28.0
psutil>=5.9.0
packaging>=21.0

# Development/Testing (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0

# Performance Monitoring
py-cpuinfo>=9.0.0